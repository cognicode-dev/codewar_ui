import type { EditorSettings } from '@/types'

export const defaultEditorSettings: EditorSettings = {
  fontSize: 14,
  tabSize: 2,
  minimap: true,
  wordWrap: false,
  lineNumbers: true,
  theme: 'vs-dark',
  language: 'typescript',
}
